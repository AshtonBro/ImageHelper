<Page
    x:Class="AshtonBro.ImageHelper.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AshtonBro.ImageHelper"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Image x:Name="ImageTarget" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center" Stretch="Uniform"/>
        <StackPanel Grid.Row="1" Margin="10" HorizontalAlignment="Center" Orientation="Horizontal" >
            <Button x:Name="CaptureButton" Content="Capture New Image" Click="CaptureButton_Click"/>
            <Button x:Name="SaveButton" Content="Save Image" Click="SaveButton_Click" Margin="20 0 0 0"/>
        </StackPanel>
    </Grid>
</Page>
